/**
 * Re-export do container DI e do @Injectable do framework interno.
 * Services dependem de abstrações (interfaces) resolvidas pelo container.
 */
export { container, Injectable } from "@mateusseiboth/ts-decorators";
