{
  "name": "capiva-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Capiva Cloud — plataforma que democratiza o acesso e o deploy em Kubernetes.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "build": "bun run --cwd backend build && bun run --cwd frontend build",
    "typecheck": "bun run --cwd backend typecheck && bun run --cwd frontend typecheck",
    "test": "bun run --cwd backend test",
    "prisma:generate": "bun run --cwd backend prisma:generate",
    "prisma:migrate": "bun run --cwd backend prisma:migrate",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
